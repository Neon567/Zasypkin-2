// Создаем два массива
let array1 = ['Яблоко', 'Банан', 'Апельсин'];
let array2 = ['Томат', 'Огурец', 'Перец'];

// Объединяем их с помощью метода concat
let combinedArray = array1.concat(array2);

// Выводим полученный массив на экран
console.log(combinedArray);
