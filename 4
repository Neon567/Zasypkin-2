// Предположим, что у нас есть следующий массив
let items = ['item1', 'item2', 'item3', 'item4', 'item5'];

// Используем цикл for для вывода каждого элемента массива
for (let i = 0; i < items.length; i++) {
  console.log(items[i]);
}
